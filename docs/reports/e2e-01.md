# E2E Report 01

## Environment

| Item | Value |
|---|---|
| Date | 2026-09-26 |
| Jenkins image | `jenkins/jenkins:2.568.3-lts-jdk21` (digest `sha256:c1e4c349365f6d16d88595b2c5f7e8ff39b8ae1d061f62420bac193b4b9616d0`) |
| Jenkins version read from the boot log | `jenkins.model.Jenkins#<init>: Starting version 2.568.3` — exactly the `jenkins.version` of `pom.xml` |
| Plugin under test | `batch-control` `1.0.0-SNAPSHOT (private-329622be-Yongjun Hong)`, `target/batch-control.hpi`, 199 543 bytes |
| Plugin state | `/pluginManager/api/json?depth=1`: `batch-control` `active=true enabled=true`; **53 plugins installed, 0 inactive** |
| Dependencies installed | `structs 362.va_b_695ef4fdf9`, `cloudbees-folder 6.1106.v3a_d9a_6d2465e` (both from the `.hpi` manifest `Plugin-Dependencies`), plus `matrix-auth 3.3` and `workflow-aggregator 608.v67378e9d3db_1` for the scenarios |
| Authorization strategy | `BatchControlAuthorizationStrategy` wrapping `hudson.security.ProjectMatrixAuthorizationStrategy` |
| Accounts | `admin` (Administer), `approver` (Read, Job/Read, BatchControl Approve + ViewHistory), `requester` (Read, Job/Read, Job/Build, BatchControl Request + RequestGrant, **no Job/Configure**) |
| Jobs | `batch-daily` (Freestyle, `DATE`/`MODE` parameters, approval required), `batch-pipeline` (Pipeline, approval required), `batch-cron` (Freestyle, `* * * * *`, not controlled) |
| Environment source | `e2e/` (`Dockerfile`, `plugins.txt`, `docker-compose.yml`, `init.groovy.d/`, `scripts/`), see `e2e/README.md` |

This report covers the environment build, the startup-log review and the whole
HTTP/REST layer. The purely visual checks are listed in
*Visual pass — handed to orchestrator* and were not executed here: this agent has
no browser tool.

## Result summary: PASS 6 / FAIL 3 / BLOCKED 0

| ID | Result | Layer covered here | Screenshot (visual pass) | Note |
|---|---|---|---|---|
| T-E2E-01 | PASS | full flow over HTTP | `e2e/screenshots/T-E2E-01-*.png` | request -> approve -> build #2 SUCCESS, cause `APPROVED_REQUEST`, dashboard links the request id |
| T-E2E-02 | PASS | markup | `e2e/screenshots/T-E2E-02.png` | "Request Run" twice, "Build Now" zero times on controlled jobs; `batch-cron` keeps "Build Now" |
| T-E2E-03 | **FAIL** | full flow over HTTP | `e2e/screenshots/T-E2E-03.png` | an active CONFIGURE grant does **not** open the job configuration screen (403). See E2E-D1 |
| T-E2E-04 | PASS | full | `e2e/screenshots/T-E2E-04.png` | 4 CSVs, correct headers, parameter column present, `text/csv` + `Content-Disposition` |
| T-E2E-05 | **FAIL** | markup | `e2e/screenshots/T-E2E-05.png` | job, requester, reason, raw parameters and both decision buttons are on one page; the job's recent run results are **not**. See E2E-D3 |
| T-E2E-06 | **FAIL** | full | `e2e/screenshots/T-E2E-06.png` | the refused save is a generic 403 with no expiry explanation and no re-request link. See E2E-D2 |
| T-E2E-07 | PASS | markup + follow-through | `e2e/screenshots/T-E2E-07.png` | dashboard row links to `/batch-control/requests/<id>/`, the detail page carries the same id |
| T-E2E-08 | PASS | full | `e2e/screenshots/T-E2E-08.png` | warning rendered, and a submit with no approver is refused with HTTP 400 "must be designated" |
| T-10-06 | PASS | full | `e2e/screenshots/T-10-06.png` | 50 rows on page 1, `?days=7&page=2` for the rest, the seeded 8-day-old record is excluded |

Additional checks that are not matrix rows but were run on this environment:

| Check | Result |
|---|---|
| Startup log review, first boot (empty `JENKINS_HOME`) | PASS — 0 SEVERE, 0 plugin warnings |
| Startup log review, second boot (existing store: 59 run records, 8 requests, 4 grant requests) | PASS — 0 SEVERE, 0 plugin warnings |
| Permission enforcement under matrix-auth (3 negative cases) | PASS — 403 each, server state unchanged |
| Direct build attempt on a controlled job | PASS — HTTP 400 with guidance, no build, empty queue |
| Grant survives a container restart | PASS — still active after restart, a save under it succeeds and is recorded |
| Global configuration + security + `/manage/` screens | PASS — all 10 global fields, all 5 permissions, the wrapping strategy, all 5 job-property fields |

## Startup log review

This is the part integration tests cannot do, so the whole log was read, not
grepped for known strings.

**First boot** (`docker compose up` on an empty volume, 39 lines):

* `SEVERE`: none.
* `WARNING`: exactly one, and it is not ours —
  `o.e.j.ee9.nested.ContextHandler#setContextPath: Empty contextPath` (Jetty,
  emitted before Jenkins starts, present on a stock image too).
* No exception, no stack trace, no `Failed to load` / `Failed Inspecting plugin`
  line. Init milestones ran in order through
  `Completed initialization` -> `Jenkins is fully up and running` in 5.7 s.
* Nothing from `io.jenkins.plugins.batchcontrol` at all: no descriptor
  registration complaint, no `Initializer` failure, no
  `ExtensionFinder` warning. `BatchControlPermissions.initialize()` (the
  `@Initializer(after = PLUGINS_STARTED)` that forces permission-group
  registration) ran silently, and the permission group is present on the
  security screen, so it worked.

**Second boot** (`docker compose restart` with a populated store — 59 run
records, 8 run requests, 4 grant requests, 19 change records, 11 diff files):
same picture. 0 SEVERE, 0 plugin warnings, one Jetty `contextPath` warning,
`Completed initialization` reached in 2.9 s. `StartupRecovery` (which runs at
`JOB_CONFIG_ADAPTED` and coordinates under `Queue.withLock`) produced no log
output and no stall — the init-graph risk noted in `docs/HANDOFF.md`
(JENKINS-37759) did not materialise.

**Whole-session log** (46 lines before the restart, 90 after): the only
plugin-related lines besides the e2e bootstrap are informational Jetty
`While serving ...: hudson.security.AccessDeniedException3` entries for the
denials the tests provoked, e.g.

```
INFO o.e.j.e.n.ContextHandler$APIContext#log: While serving
http://localhost:8080/batch-control/requests/20260926-140340-3yk87g/approve:
hudson.security.AccessDeniedException3: requester is missing the Batch Control/Approve permission
```

No plugin-originated exception was logged during any scenario, including the
ones that ended in 400/403.

## FAIL detail

### E2E-D1 (T-E2E-03) — an active CONFIGURE grant does not open the job configuration screen

**Severity: high.** This breaks the acceptance criterion of SPEC item 8 for the
path a human actually takes ("승인되면 그 시간 동안 본인 계정으로 직접 작업하며").
The integration tests miss it because they assert on `hasPermission(Item.CONFIGURE)`
or on the `config.xml` POST, both of which pass.

Reproduction (`e2e/scripts/rest-grant-configure.sh`):

1. `requester` has no `Job/Configure`. `GET /job/batch-daily/configure` -> **403** (correct baseline).
2. `requester` POSTs `/batch-control/grants/create` with `scopeType=JOB`,
   `scopeFullName=batch-daily`, `actions=CONFIGURE`, `durationMinutes=1` -> **302**.
3. `approver` POSTs `/batch-control/grants/<id>/approve` -> **302**. The grant is now active.
4. `GET /job/batch-daily/configure` as `requester` -> **403**
   `requester is missing the Job/Configure permission`  <- **expected 200**
5. `POST /job/batch-daily/config.xml` as `requester` -> **200**, the change is
   applied and recorded (`changes.csv`:
   `20260926-140503-ds891c,CONFIGURE,batch-daily,requester,...,20260926-140503-cz7hy3,` —
   the `grantId` column is correctly linked).
6. `GET /job/batch-daily/config.xml` as `requester` -> **403** as well.

Expected: inside the window the requester can open and save the job
configuration. Actual: only the raw `config.xml` POST works; every read path of
the configuration UI is refused, so a browser user cannot use the grant at all.

**Root cause, confirmed on the running instance** (admin script console,
`e2e/out/probe2.groovy` / `probe3.groovy`):

```
hasActiveGrant(requester, batch-pipeline, Item.CONFIGURE) = true
as requester: batch-pipeline ACL=GrantAwareACL CONFIGURE=true EXTENDED_READ=false READ=true
Item.EXTENDED_READ.enabled  = false
Item.EXTENDED_READ.impliedBy = Permission[interface hudson.model.Item,Configure]
checkPermission(EXTENDED_READ) threw: AccessDeniedException3: requester is missing the Job/Configure permission
checkPermission(CONFIGURE): OK
```

`Item.EXTENDED_READ` is disabled by default in core, so core resolves a check
for it through its `impliedBy` chain (and reports the failure under the name of
the implying permission, which is why the 403 says "Job/Configure" and hid the
real cause). That resolution happens **inside the delegate's `SidACL`**, where
the grant is invisible: `GrantAwareACL.hasPermission2` only consults
`GrantService` when `GrantAction.fromPermission(permission) != null`, which is
true for the three literal grantable permissions and false for everything
implied by them. The configuration screen and `GET config.xml` check
`Item.EXTENDED_READ`, so they never reach the grant.

Suggested direction (not applied — `src/main` is not this agent's path): in
`GrantAwareACL.hasPermission2`, walk the `impliedBy` chain the way core does —
for `p = permission; p != null; p = p.impliedBy`, skip disabled permissions and
consult the grant whenever `GrantAction.fromPermission(p) != null`. A regression
test should assert `job.hasPermission(Item.EXTENDED_READ)` under an active
CONFIGURE grant, and an HTTP-level test should assert `GET /job/<name>/configure`
is 200 inside the window.

### E2E-D2 (T-E2E-06) — the expired-window refusal gives the user nothing to act on

**Severity: medium** (and partly a specification question, see *Documentation defects*).

Reproduction: steps 1-3 above with `durationMinutes=1`, then wait 70 s and retry.

* `GET /job/batch-daily/configure` -> 403, body is the stock Jenkins
  "Access Denied / requester is missing the Job/Configure permission" page.
* `POST /job/batch-daily/config.xml` -> 403, and the configuration is unchanged
  (verified by re-reading `config.xml` as admin: the description is still the one
  written inside the window). The expiry itself is therefore correct and
  timer-independent, as SPEC item 8 requires.
* Content checks on the refusal body: `expired` **NO**, `grant` **NO**,
  `batch-control/grants` **NO**.

Expected by T-E2E-06: "권한 창 만료 안내가 표시되고 권한 재요청 링크가 보인다".
Actual: no mention of the permission window, no link to `/batch-control/grants/`.
Note that the *run* path does this well (see the guidance page below), so the
asymmetry is visible to users.

### E2E-D3 (T-E2E-05) — the decision screen has no recent-run context

**Severity: medium.** `GET /batch-control/requests/<id>/` as `approver` returns
200 and contains, verified by content check: `batch-daily`, `requester`, the full
reason text, `DATE`/`2026-09-20`, `MODE`/`partial`, and both decision endpoints.
Its headings are exactly:

```
<h1>Run Request 20260926-141127-qo7ejj</h1>
Job | Status | Requester | Approver | Created | Decided
<h2>Reason</h2> <h2>Parameters</h2> <h2>Decision</h2>
```

There is no section for the job's recent runs and no link to a build — the only
outgoing job link is `/job/batch-daily/`. T-E2E-05 requires "해당 잡의 최근 실행
결과가 한 화면에 함께 표시된다", so the row fails on that one element. The
approver must open the job in another tab to see whether the previous run
succeeded.

## Evidence for the passing rows

Status codes and response substance, as produced by the scripts.

**T-E2E-01** (`rest-run-request.sh`):

```
GET  /job/batch-daily/batch-control/            (requester) -> 200
POST /job/batch-daily/batch-control/submit      (requester) -> 302  Location: /batch-control/requests/20260926-140251-iedhl4/
GET  /batch-control/requests/<id>/              (approver)  -> 200
POST /batch-control/requests/<id>/approve       (approver)  -> 302
GET  /job/batch-daily/lastBuild/api/json        (admin)     -> 200  "number":2 "result":"SUCCESS" "building":false
   cause:      "Approved batch run request 20260926-140251-iedhl4 (requested by requester, approved by approver)"
   parameters: "name":"DATE","value":"2026-09-26"  "name":"MODE","value":"full"
GET  /batch-control/dashboard/                  (admin)     -> 200  contains the request id and an APPROVED_REQUEST row
GET  /batch-control/requests/<id>/              (admin)     -> 200  status EXECUTED
```

**Permission enforcement** (`rest-permission-denied.sh`) — the point of this
block is that the integration suite proved it with the test harness's ACL; here
it is the real `matrix-auth` strategy under the plugin's wrapper:

```
POST /job/batch-daily/batch-control/submit  as approver  -> 403  "requester is missing ..." / "is missing the Batch Control/Request permission"
GET  /job/batch-daily/batch-control/        as approver  -> 403  (the getTarget gate on the whole subtree)
POST /batch-control/requests/<id>/approve   as requester -> 403  "is missing the Batch Control/Approve permission"
GET  /batch-control/history/                as requester -> 403
GET  /batch-control/history/runs.csv        as requester -> 403
state before: 4 run requests, 2 builds   state after: 5 run requests (the one (b) legitimately created), 2 builds
the request (b) tried to self-approve: still PENDING
```

**T-E2E-04** (`rest-csv-export.sh`): all four exports 200,
`Content-Type: text/csv;charset=UTF-8`,
`Content-Disposition: attachment; filename="runs.csv"`, `X-Content-Type-Options: nosniff`.

```
runs.csv     header runId,jobFullName,number,causeType,user,parameters,result,startedAt,durationMs,abortedBy,runRequestId
             row    batch-daily#2,batch-daily,2,APPROVED_REQUEST,,"DATE=2026-09-26, MODE=full",SUCCESS,2026-09-26T05:02:52.320Z,2008,,20260926-140251-iedhl4
requests.csv header id,jobFullName,parameters,reason,requester,approver,status,createdAt,decidedAt,decisionComment,selfApproved,incidentId,executedRunId
             row    20260926-141127-qo7ejj,batch-daily,"DATE=2026-09-20, MODE=partial",T-E2E-05 rerun ...,requester,approver,PENDING,...
changes.csv  header id,type,target,user,at,grantId,detail                (19 rows)
incidents.csv header id,runId,jobFullName,result,status,createdAt,resolvedByRunId,rerunRequestIds   (0 rows, no failing build occurred)
```

The parameter column is present and populated in both `runs.csv` and
`requests.csv`. Row count scales with the runs performed (the matrix row says
"10 runs -> 10 rows + header"; here 58 in-window runs produced 58 data rows plus
the header).

**T-E2E-08** (`rest-approvers-empty.sh`): with `approvers = []`,
`GET /job/batch-daily/batch-control/` -> 200 and renders
`<div class="jenkins-alert jenkins-alert-warning">No approvers are configured. Ask an
administrator to add approvers in the Batch Control global configuration.`
A submit anyway -> **400** "... must be designated." Approvers restored
afterwards by the same script.

**T-10-06** (`rest-dashboard.sh`): 55 builds of `batch-cron` triggered by
`requester` (all accepted — an uncontrolled job still builds freely while run
control is on), plus 1 timer build and the 2 approved runs = 58 records.

```
GET /batch-control/dashboard/          -> 200, 51 <tr> (1 header + 50 rows), link ?days=7&page=2
GET /batch-control/dashboard/?days=7&page=2 -> 200, the remaining 8 rows
notice on the page: "Showing the last 7 day(s), newest first. Secret parameter values are stored masked."
```

A record with `startedAt` 8 days in the past was appended to
`$JENKINS_HOME/batch-control/runs/2026-09.jsonl`
(`batch-cron#9001`): it does **not** appear in the default dashboard view and
does appear in `runs.csv`, which matches the documented S-12 behaviour (history
and CSV are outside the dashboard window).

**Direct build attempt** (`rest-blocked-build.sh`) — the PoC "assumption D"
guidance page, never seen outside tests before:

```
POST /job/batch-daily/buildWithParameters?DATE=2026-03-03&MODE=full  as requester -> 400
  <h1>Error</h1>
  <p>Approval required: job 'batch-daily' only runs through an approved batch-control run
     request. Submit a run request at http://localhost:8080/job/batch-daily/batch-control/
     and wait for approval.
builds after: 2 (unchanged)   queue: {"items":[]}
```

(`POST /job/batch-daily/build` without a form submission is refused by core with
its own 400 "This page expects a form submission" before the plugin is reached —
core behaviour for a parameterized job, not a finding.)

**Administration screens** (`rest-admin-screens.sh`):

* `GET /manage/configure` -> 200 with all ten global fields present
  (`runControlEnabled`, `changeControlEnabled`, `approversText`,
  `allowAdminSelfApproval`, `pendingTimeoutHours`, `approvedRunTimeoutMinutes`,
  `grantDurationOptionsText`, `maxGrantMinutes`, `incidentResultsText`,
  `retentionMonths`).
* `GET /manage/configureSecurity/` -> 200 mentioning `Batch Control`, `Manage`,
  `Request`, `Approve`, `RequestGrant`, `ViewHistory`,
  `BatchControlAuthorizationStrategy` and its display name
  `Batch Control (wrapping)`. This is the raw-DOM evidence the T-02-02 note asked
  to confirm visually.
* `GET /manage/` -> 200, sidebar entry "Batch Control" -> `/batch-control`. The
  only active administrative monitors are core's
  (`CspRecommendation`, `ReverseProxySetupMonitor`, `RootUrlNotSetMonitor`,
  `URICheckEncodingMonitor`, `ControllerExecutorsNoAgents`); **no batch-control
  monitor is activated**, which is the correct negative state here — the delegate
  gives standing change permissions to `admin` only (exempt) and Role Strategy is
  not installed.
* `GET /job/batch-daily/configure` -> 200 with all five job-property fields.

**Restart survival**: a 60-minute CONFIGURE grant on `batch-pipeline` was
approved, then `docker compose restart jenkins`. After the restart
`hasActiveGrant(requester, batch-pipeline, Item.CONFIGURE) = true`,
`POST /job/batch-pipeline/config.xml` as `requester` -> 200, and the change was
recorded with the `grantId` column filled. The dashboard still showed its 50 rows
per page.

## Visual pass — handed to orchestrator

Everything below needs eyes on a rendered page, which this agent cannot do. The
environment is up and in the right state; take the rows in order. Log in through
`http://localhost:8080/login`. Use a separate browser profile/context per
account — do not log out and back in within one context.

Passwords are in `e2e/.env` (git-ignored): `admin` / `$BC_ADMIN_PASSWORD`,
`approver` / `$BC_APPROVER_PASSWORD`, `requester` / `$BC_REQUESTER_PASSWORD`.

| Row | Account | URL / clicks | Look at | Screenshot |
|---|---|---|---|---|
| V-01 (T-E2E-02, T-06-15) | requester | `http://localhost:8080/job/batch-daily/` | the sidebar: **no "Build Now"**; a "Request Run" entry with the paper-plane icon. Note there are **two** "Request Run" entries (see UX-1) | `e2e/screenshots/T-E2E-02.png` |
| V-02 (T-E2E-01 step 1) | requester | from V-01 click the **lower** "Request Run" (`/job/batch-daily/batch-control/`) | the request form: `DATE`, `MODE` rendered by their own parameter views, an approver select holding `approver`/`admin`, a reason field | `e2e/screenshots/T-E2E-01-1.png` |
| V-03 (T-E2E-01 step 2) | requester | fill reason + parameters, submit | landing on the request detail page, status PENDING | `e2e/screenshots/T-E2E-01-2.png` |
| V-04 (T-E2E-01 step 3, T-E2E-05) | approver | `http://localhost:8080/batch-control/requests/` then open the PENDING request | whether one screen is enough to decide: job, requester, reason, raw parameters, approve/reject. **Confirm the absence of the job's recent run results (E2E-D3)** | `e2e/screenshots/T-E2E-05.png` |
| V-05 (T-E2E-01 step 4) | approver | click Approve | the confirmation, then that the request becomes APPROVED/EXECUTED and names the build | `e2e/screenshots/T-E2E-01-3.png` |
| V-06 (T-E2E-07) | admin | `http://localhost:8080/batch-control/dashboard/` -> click the request id on an `APPROVED_REQUEST` row | the row layout (job, cause, user, parameters, result, duration), and that the link lands on the matching request detail | `e2e/screenshots/T-E2E-07.png` |
| V-07 (T-10-06) | admin | same page, then the "Older" link | "Showing the last 7 day(s)" notice, 50 rows, the paging control (there is no page number or total count — UX-5) | `e2e/screenshots/T-10-06.png` |
| V-08 (T-E2E-04) | admin | `http://localhost:8080/batch-control/history/` -> the CSV export links | whether the four exports are discoverable and labelled, and that a click downloads `runs.csv` | `e2e/screenshots/T-E2E-04.png` |
| V-09 (PoC assumption D) | requester | `http://localhost:8080/job/batch-daily/` -> click the **upper** "Request Run" (core's relabeled build link) -> fill the parameters -> **Build** | the guidance page: "Approval required: job 'batch-daily' only runs through an approved batch-control run request. Submit a run request at <url> and wait for approval." Note the URL is plain text, not a link (UX-2) | `e2e/screenshots/blocked-build-guidance.png` |
| V-10 (T-E2E-03, defect) | requester | a CONFIGURE grant on `batch-daily` is already active (created by `e2e/scripts/grant-setup.sh batch-daily 30` at 14:19; if it has lapsed, re-run that script) -> `http://localhost:8080/job/batch-daily/configure` | the **403 "Access Denied ... missing the Job/Configure permission"** page although the grant is active — visual confirmation of E2E-D1 | `e2e/screenshots/T-E2E-03.png` |
| V-11 (T-E2E-06, defect) | requester | `http://localhost:8080/batch-control/grants/` -> submit CONFIGURE on `batch-daily` for **1 minute**; approver approves; wait 70 s; then open `/job/batch-daily/configure` | that the refusal explains nothing about the expired window and offers no re-request link | `e2e/screenshots/T-E2E-06.png` |
| V-12 (T-E2E-08) | requester | run `bash e2e/scripts/approvers-clear.sh`, open `http://localhost:8080/job/batch-daily/batch-control/`, screenshot, then run `bash e2e/scripts/approvers-restore.sh` | the warning alert renders as a warning (colour, placement) and the approver select is not offered as an empty dropdown | `e2e/screenshots/T-E2E-08.png` |
| V-13 (T-02-02) | admin | `http://localhost:8080/manage/configureSecurity/` -> expand the authorization matrix card | the **"Batch Control" permission group** with its five columns (Manage, Request, Approve, RequestGrant, ViewHistory) visible to a human, and `Batch Control (wrapping)` in the strategy list. This closes the deferred DOM-level assertion | `e2e/screenshots/T-02-02.png` |
| V-14 (global configuration) | admin | `http://localhost:8080/manage/configure` -> the "Batch Control" section | field labels, ordering, and that every help icon opens text | `e2e/screenshots/global-config.png` |
| V-15 (job property) | admin | `http://localhost:8080/job/batch-daily/configure` -> the "Batch Control" property | the five checkboxes/fields and their help | `e2e/screenshots/job-property.png` |
| V-16 (grants screen) | requester | `http://localhost:8080/batch-control/grants/` | the new-request form (scope type, scope name, action checkboxes, duration select with 1/15/30/60, custom duration, reason, approver), the request list and the active-grant list with a remaining-time indication | `e2e/screenshots/grants.png` |
| V-17 (empty-state text) | approver | `http://localhost:8080/batch-control/incidents/` (no incident exists) | what an empty list tells the user | `e2e/screenshots/incidents-empty.png` |

## Documentation defects

1. **`README.md` is the development-kit README, not the plugin's.** It describes
   the agent kit ("빈 디렉터리를 만들고 이 키트의 내용을 그대로 복사합니다") and
   contains no installation or configuration procedure, so the instruction to
   "follow README and verify the document is right" could not be executed —
   there is nothing to follow. It is also in Korean, against the English-artifact
   rule for repository output.
2. **Nothing documents that JIT change control needs the plugin's authorization
   strategy to be selected.** With Matrix Authorization left as the strategy,
   every grant is approved and then has no effect whatsoever; the only statement
   of this requirement lives in a Javadoc comment and in
   `docs/ARCHITECTURE.md` section 4. An administrator following the (missing)
   setup documentation would conclude the feature is broken. This needs a
   "Setup" section: install, enable the switches, select
   `Batch Control (wrapping)` with the real strategy as delegate, assign the five
   permissions.
3. **Dependencies for a working installation are undocumented.** The `.hpi`
   manifest requires only `structs` and `cloudbees-folder`, but the five
   permissions cannot be assigned at all without a matrix/role-style
   authorization strategy (`matrix-auth` here). Worth stating, together with the
   `RoleStrategyNoticeMonitor` limitation.
4. **SPEC item 8 does not say what a user sees when the window has expired.**
   T-E2E-06 expects an expiry notice and a re-request link, which is a
   reasonable product decision but is not in the SPEC acceptance criteria. Either
   the SPEC gains that criterion (and E2E-D2 is a real defect) or the matrix row
   is relaxed. This is a human decision, not an agent one.

## UX observations (for human judgement — not defects)

* **UX-1: two sidebar entries labelled "Request Run" that go to different
  places.** On a controlled job the requester sees the plugin's "Request Run"
  action *and* core's build link, which `RequestRunUiDecorator` relabels to the
  same words. The core one leads to the build-parameters form and, on submit, to
  the "Approval required" error page — a dead end that looks like the right
  button. Consider a distinct label for the decorated core link (for example
  "Build Now (approval required)") or removing/redirecting it.
* **UX-2: the guidance message prints a bare URL** ("Submit a run request at
  http://…/batch-control/ and wait for approval") instead of a hyperlink. The
  user has to select and copy it.
* **UX-3: the expired-grant refusal drops the user with no next step** (E2E-D2),
  while the blocked-run path explains itself well. The asymmetry is the thing a
  user will notice.
* **UX-4: `runs.csv` leaves the `user` column empty for approved runs.** The row
  is `batch-daily#2,batch-daily,2,APPROVED_REQUEST,,"DATE=…",SUCCESS,…,20260926-140251-iedhl4`:
  who asked for it is only recoverable by joining `requests.csv` on
  `runRequestId`. Timer and user builds do fill the column. An auditor reading
  one file will read it as "nobody".
* **UX-5: the dashboard pager has no position information** — only an "Older"
  link, no page number, no total count, so a reader cannot tell how much is
  behind it. Row count per page is 50 as specified.
* **UX-6: the decision screen forces a context switch** (E2E-D3). Even a
  three-line "last 3 runs of this job" block would remove the second tab.
* **UX-7: change records for jobs created by automation show `SYSTEM` as the
  user**, with an empty `grantId`. Correct, but a reader auditing "changes
  without a grant" will see rows they cannot attribute to a person; a short note
  in the change screen would help.
* **UX-8: the request detail page shows both `APPROVED` and `EXECUTED` wording in
  the same area** ("EXECUTED", "Executed", "approved"), which reads slightly
  redundantly in the HTML; worth a look at the rendered page.

## Environment notes worth keeping

Two traps cost time here and are recorded in the environment files so they are
not rediscovered:

1. **Do not bind-mount the `.hpi` into `/var/jenkins_home/plugins/`.** Docker
   creates the parent directory as `root`, and Jenkins (running as `jenkins`)
   then aborts the whole init reactor:
   `SEVERE Failed Inspecting plugin /var/jenkins_home/plugins/batch-control.jpi`
   / `java.nio.file.AccessDeniedException: /var/jenkins_home/plugins/batch-control`,
   which also cascades into a misleading
   `ClassNotFoundException: …BatchControlGlobalConfiguration` from the init
   scripts (no plugin was started, so the uber class loader had nothing). The
   mount target is `/usr/share/jenkins/ref/plugins/batch-control.jpi.override`
   instead; the `.override` suffix makes `jenkins.sh` copy it over an existing
   plugin on every boot.
2. **A trigger added programmatically never fires until it is started.**
   `job.addTrigger(new TimerTrigger('* * * * *')); job.save()` leaves the trigger
   registered but dead (`builds: []` forever). `timer.start(job, true)` after the
   save fixes it.

Also note that init scripts in `init.groovy.d` are compiled by Jenkins itself, so
`import`ing a plugin class fails; plugin classes are loaded through
`Jenkins.get().pluginManager.uberClassLoader` in all three bootstrap scripts.

## 요청: src/main/java/io/jenkins/plugins/batchcontrol/security/GrantAwareACL.java (core-dev)

E2E-D1. `hasPermission2` consults `GrantService` only when
`GrantAction.fromPermission(permission) != null`, so permissions *implied by*
`Item.CONFIGURE`/`CREATE`/`DELETE` are never granted. Because `Item.EXTENDED_READ`
is disabled in core and is resolved through its `impliedBy` chain inside the
delegate's `SidACL`, the job configuration screen and `GET config.xml` are 403
for a user holding an active CONFIGURE grant, while `POST config.xml` succeeds.
Suggested fix: walk the `impliedBy` chain (skipping disabled permissions) and
consult the grant for each grantable permission on the way up. Evidence and
reproduction are in the FAIL detail above.

## 요청: src/test (test-author)

Add regression coverage for E2E-D1 once it is fixed: (a) a unit/integration
assertion that `job.hasPermission(Item.EXTENDED_READ)` is true under an active
CONFIGURE grant, and (b) an HTTP-level assertion that `GET /job/<name>/configure`
returns 200 inside the window and 403 after it. The current suite passes with the
bug present, which is the interesting part.

## 요청: src/main/java/io/jenkins/plugins/batchcontrol (core-dev / ui-dev, after a human decision)

E2E-D2 (T-E2E-06). Decide whether an expired permission window must produce its
own guidance page with a re-request link, mirroring
`ApprovalQueueDecisionHandler`'s message on the run path. If yes, the refusal has
to be turned into a plugin-rendered response instead of core's stock 403 page.
See *Documentation defects* item 4 — the SPEC currently does not require it.

## 요청: src/main/java/io/jenkins/plugins/batchcontrol/action/RequestItem.java + src/main/resources/.../RequestItem/index.jelly (ui-dev)

E2E-D3 (T-E2E-05). The decision screen needs the job's recent run results
(result, number, time, link) so an approver can decide without leaving the page.

## 요청: README.md (release-manager)

Documentation defects 1-3: the repository README is the development-kit README.
The plugin needs a user-facing README in English with an installation section, a
setup section that states the `Batch Control (wrapping)` authorization strategy
requirement and the five permissions, and a note that a matrix/role-style
strategy is needed to assign them (with the Role Strategy limitation).

## 요청: docs/SPEC.md (human)

Documentation defect 4: SPEC item 8 has no acceptance criterion for what the user
is told when a permission window has expired, while T-E2E-06 expects a notice and
a re-request link. Please settle which one moves.

## 요청: docs/TEST-MATRIX.md (test-author, informational)

T-E2E-04's precondition says "실행 10건 -> 10행 + 헤더". The row was verified with
58 in-window runs producing 58 data rows plus the header; the exact count is a
function of how much the environment has run, so the criterion is better phrased
as "one row per run in scope, plus a header, with the parameter column
populated". No behaviour change implied.
